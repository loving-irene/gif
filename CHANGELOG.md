# CHANGELOG

## 2026-09-14 · 0.1.49

- 统一管理后台页签按钮的默认、悬停与选中样式，沿用米白与棕色主题；任务与兑换码状态使用统一标签，表格表头、间距与分页样式一致。
- 后台各列表及数据看板表格改为单行省略号截断，点按数据可打开完整内容弹窗并选择复制；固定时间与操作列宽，保留原始完整数据及配置文本域的多行编辑能力。
- 手机端采用紧凑表格：收紧间距和列宽、固定首列、表格内部横向滚动并显示滑动提示；9个页签以三列排列，统一44px按钮触控高度，表单字号16px，分页和看板同步适配窄屏。
- 后台最终资源升级为 `style.v31.css` 与 `admin.v26.js`。

## 2026-09-14 · 0.1.48

- 修复任务输入写盘失败时生成槽位未释放的问题，磁盘异常不再逐步耗尽全部生成槽位；创建被拒或事务失败后立即删除暂存输入，避免未创建任务的照片残留。
- 将同款检测、请求幂等检查与入队放在同一提交临界区；两个相同配置并发提交时仍需二次确认，同一请求编号并发重传返回同一任务，避免重复扣次或误报创建失败。图片校验与实际上游生成不占用提交锁。
- 统一在线失败与超时清理的收口流程：任务终态、余额退款、扣次字段及消耗统计同事务更新，退款依据提交时保存的策略，后台中途改配置不影响在途任务。数据库写入失败整体回滚，重复清理不会重复退款；缓存淘汰后仍返回正确扣次状态，超时清理同步更新内存状态并删除输入。
- 调度器只认领排队与等待上游结果的任务，避免将已被清理的终态任务重新启动。调用记录裁剪保留在途任务，列表仍最多返回最近100条。
- 服务关停等待生成协程退出后再关闭数据库；关停取消保留在途任务输入、上游任务号及已扣次数供重启恢复，不再因取消而提前判失败或删除恢复输入。
- 新增任务生命周期回归测试，覆盖磁盘异常恢复、被拒任务输入清理、并发去重、退款策略固定、退款事务回滚、重复超时清理、缓存丢失、终态不可重启、在途记录保护和关停等待。已有重启测试桩改为响应上下文取消，与实际HTTP请求行为一致。
- 新增 `npm test`，5组前端测试跟随首页实际资源引用，修复旧测试仍引用 v6/v9/v21 脚本及过时计时函数的问题。同步 README 中图片保存、20分钟任务预算与检查命令；首页保存提示、FAQ、结构化数据与 llms.txt 改为现有跨设备同步、定稿按数量保留和社区独立保存的实际规则，本次未修改样式资源。

## 2026-09-14 · 0.1.47

- 窄屏（≤800px，手机与竖屏平板）页头改成上下两行：第一行只放品牌标识（✳ 拾光 GIF 与副标题），第二行从「社区」起排列全部入口（社区、分享、反馈、我的作品、增加次数、剩余次数、账户）。此前品牌与入口挤在同一条 flex 行里：手机（≤600px）只能靠隐藏「我的作品」「增加次数」腾地方，601–800px 则把「分享」「反馈」「我的作品」「增加次数」「次创作」的文字都挤成两行（700px 实测五个入口各自折行）；现在入口行占满整行、行内两端分布，品牌图标与社区入口左侧对齐、账户头像落在右边缘。宽屏（>800px）布局不变。
- 只调页头的纵向内边距（`padding-top` / `padding-bottom`），左右内边距继续由 `.wrap` 提供，页头与下方正文仍左右对齐；高度由固定的 104px（手机 83px）改为自适应，避免把两行内容压扁。
- 首页样式递增为 `style.v29.css`（基于主线现用的 `style.v27.css` 追加窄屏页头双行规则，v28 属后台专用样式线，不复用），`index.html` 与测试引用同步更新。新增 Go 测试 `TestNarrowHeaderStacksLogoAboveEntries`：钉住页头品牌在前、入口 `nav` 在后且品牌不在 `nav` 内，双行断点为 800px，`.header` 纵向排列且高度自适应、`.header nav` 占满整行并两端分布，且该断点不得出现 `padding` 简写（否则会清掉 `.wrap` 的左右内边距导致页头与正文错位）。

## 2026-09-14 · 0.1.46

- 修复首页页头「个人账户」（☺ 头像按钮）在生成任务进行时点不开的问题：`updateControls()` 里一直有 `$("accountBtn").disabled = tasks.size > 0`，只要有定稿或动作在制作，整个账号入口就被禁用成灰按钮——任务可能跑好几分钟，刷新页面后恢复查询的任务最长还会持续 30 分钟，这段时间里用户点不开账号弹窗，看不到剩余次数、也做不了邮箱登录与兑换。现在账号入口不再禁用，任何任务在跑都能打开弹窗。
- 「切换账号会丢任务」的保护挪到弹窗内部：只有真会丢任务的「退出当前账号」在任务进行时禁用（服务端仍会做完并保留 3 天，但换成新设备账号后再也找不回来），并在弹窗顶部用 `#accountLockedHint` 说明原因与任务数；保存账户名、查看剩余次数与调用记录、关联邮箱登录都不受影响（邮箱登录是账号合并，进行中的任务会跟着并入合并后的账号，不会失去归属）。每次打开弹窗都会按当时的任务状态刷新这行说明。
- 首页脚本递增为 `app.v32.js`，`index.html` 与测试引用同步更新。新增 Go 测试 `TestHomepageAccountEntryStaysClickable`：首页脚本不得再出现 `$("accountBtn").disabled`，账号弹窗必须带 `#accountLockedHint`，且 `syncAccountActions()` 仍在弹窗内按 `tasks.size > 0` 禁用 `#logoutBtn`。

## 2026-09-13 · 0.1.45

- 修复首页「示例作品」永远停在静态首帧、看起来不像动图的问题：挂载点是 `<picture class="showcase-media">`，而动画层是脚本运行时 `append` 进去的——浏览器不会播放运行时插入 `<picture>` 的图片，两份示例因此一直只有静态首帧（已用最小用例在 Chrome 里复现：同一张动画图放进 `div` 会动，放进 `picture` 一定不动）。现在把两处挂载点改成普通 `<div class="showcase-media">`，`.showcase-media` 自身的 `position: relative` / `aspect-ratio: 1` / `overflow: hidden` 与内层 `img` 规则都继续适用，动画层照旧绝对定位覆盖在首帧上。
- 示例动画改为带版本号的独立文件 `example-heart.v2.webp` / `example-success.v2.webp`（内容不变，仍是 256×256、16 帧、1.4 秒循环），旧的 `example-*.webp` 下线；`/assets/` 是 `immutable` 长期缓存，换文件名后已缓存页面也能拿到可播放的示例与修复。
- 新增两条页面断言：首页不得出现 `<picture>`，且挂载点必须是 `<div class="showcase-media">`，避免以后再改回不播放动画的写法。

## 2026-09-13 · 0.1.44

- 次数不足时的创建流程更直接：「生成我的专属角色」「重新生成」「让我的角色动起来」本来就不因剩余次数禁用（禁用只看必填项、服务配置与并发上限），点击后才做次数检查；现在次数不够打开「增加次数」弹窗时，顶部会用加粗醒目的提示条说明原因（如「剩余创作次数不够，无法生成新的角色定稿。扫下方二维码添加微信即可增加次数」「已选 N 个动作，超过剩余 M 次」），紧跟着就是二维码与「添加微信多增加20%」，无需再找入口；主动点「增加次数」或「剩余次数」徽章打开时不显示原因条。
- 首页样式递增为 `style.v27.css`（新增 `.redeem-reason` 提示条）、脚本递增为 `app.v31.js`（`openRedeem` 支持传入原因文案），`index.html` 与测试引用同步更新。

## 2026-09-13 · 0.1.43

- 首页页头「获取兑换码」入口改名为「增加次数」：点击后弹窗直接展示微信二维码（与页脚「联系我」共用 `/assets/wechat.v1.jpg`，首次打开才加载），不再先看到兑换码输入框；次数不足时自动弹出的也是同一弹窗。
- 弹窗顶部新增醒目加粗提示「✦ 添加微信多增加20%」：红底红字徽章式展示（加粗 800、17px），配「打开微信扫一扫，添加好友即可获得更多创作次数」说明与「↓ 保存二维码」入口；兑换码输入表单保留在下方，供已拿到兑换码的用户直接兑换。
- 移除原「可联系微信获取兑换码 / 查看微信二维码 ↗」中转按钮（二维码已直接展示）；首页样式递增为 `style.v26.css`（新增 `.redeem-bonus`、`.redeem-qr`、`.redeem-save`，移除随之失效的 `.redeem-hint`）、脚本递增为 `app.v30.js`（新增 `openRedeem()` 统一打开弹窗并懒加载二维码），`index.html` 与测试引用同步更新。

## 2026-09-13 · 0.1.42

- 修复首页「示例作品」区在宽屏下的位置：卡片区此前被 `justify-content: center` 整块居中，在 1180px 视口里比同栏的标题、正文右移约 222px（左边缘 254px 对 32px），看起来像浮在页面中间；现在改为左对齐到内容左边缘，与「先看看，一张自拍能变成什么」齐平，卡片仍锁 348px 并排两列。≤800px 的横向滑动吸附布局不变。
- 修复示例动图的挂载位置：动画层是 `position: absolute; inset: 0`，此前被 `showcase.v1.js` 挂到未定位的 `.showcase-card`（还带 16px 内边距）上，包含块因此回落到初始包含块——动画 256×256 被画在页面左上角（盖住页头与 hero 的左上 256×256 区域），而不是盖在静态首帧上；现在挂到卡片里唯一定位的 `.showcase-media`（`position: relative` + `aspect-ratio: 1`），动画与首帧严格重合。
- 首页样式递增为 `style.v25.css`（基于 `style.v23.css`，只改宽屏 `.showcase-list` 对齐与注释，社区页继续用 `style.v24.css`）；加载器递增为 `showcase.v2.js`，`index.html` 引用同步更新。`/assets/` 是 `immutable` 长期缓存，两处都必须换文件名才能让已缓存页面拿到修复。
- 新增 Go 测试：`TestHomepageShowcaseLeftAligned` 钉住宽屏 `.showcase-list` 规则既不含 `justify-content` 也不含 `margin: auto`；`TestHomepageShowcaseOverlayAnchored` 钉住加载器把动画层挂到 `.showcase-media`（且该规则仍满足 `position: relative`、`aspect-ratio: 1` 的挂载前提）。
- 修复「点击分享提示『分享内容无效』」：前端默认不把本机 GIF 传上去，指望服务端用自己那份作品副本，但服务端副本只保留 3 天、到期即清理，客户端无从判断副本是否还在，于是服务端取不到图又没收到上传内容，只能回 400 `分享内容无效`——凡是超过 3 天的作品（含跨设备同步下来、保留期内没被清掉的本机副本）必然分享失败。现在本机有 GIF 就随请求一并上传（上限由 3MB 对齐到服务端的 4MB），服务端仍优先用自己那份副本，所以刚生成的作品分享时不会真的多传一次。
- 分享接口的错误提示改为可定位：请求体超限返回 413「这张作品太大，暂时无法分享，请换一张」，本机没有可上传的动图时返回 400「这张作品在本机还没合成为动图，请先点『重新合成 · 免费』再分享」，不再一律回笼统的「分享内容无效」。
- 首页脚本递增为 `app.v29.js`（分享上传逻辑），`index.html` 与测试引用同步更新。新增 Go 测试 `TestCommunityShareWithoutServerWorkCopy`：服务端没有作品副本时，带编号的请求靠本机 GIF 也能分享成功且社区里的动图与上传内容一致，超限请求明确回 413。

## 2026-09-13 · 0.1.41

- 作品集卡片的「原图」按钮替换为「分享」：点击即把这张动图分享到社区，服务端已有作品副本时直接复用（不用上传），本机独有的作品由页面上传 GIF；已分享的作品按钮变为「取消分享」，两种状态都只影响社区池，不动本机与云端作品。
- 页头新增「社区」入口（手机收成图标按钮），指向独立页面 `/community`：浏览所有用户主动分享出来的动图，两列卡片、进入视口才加载、离开即卸载，宽屏三到四列，支持「加载更多分享」分页；自己分享的卡片带「我分享的」标记与「取消分享」按钮。
- 新增 `community_shares` 表作为独立分享池：GIF 本体在分享时复制进来，与作品集（`works`，3 天保留）完全分离、永不清理，只由分享人主动取消；`UNIQUE(sharer,work_id)` 让同一账号同一张作品只保留一条——重复分享刷新时间与内容（条目编号不变、顶到最前），取消后可以再次分享。单账号最多 60 条、每小时最多 60 次分享，超出按时间淘汰最旧并限流。
- 新增接口：`GET /api/community`（键集分页列表，含 `nextCursor` 与「我的分享」标记）、`GET /api/community/states`（作品集卡片用已分享编号）、`GET /api/community/{id}/gif`（动图本体）、`POST /api/community/share`、`POST /api/community/unshare`（仅分享人本人可取消，他人调用返回成功但不删他人条目）。社区接口都要求登录：既用于标记归属，也便于限流；只收 GIF，静态图与超 4MB 内容被拒。
- 邮箱账号合并时分享跟随合并后的账号（先删除冲突的重复条目再改归属），换绑邮箱后分享不丢、仍可取消；`/community` 页面带 `X-Robots-Tag: noindex, follow` 且不进 sitemap，社区页样式为 `style.v24.css`、脚本为 `community.v1.js`。
- 首页样式递增为 `style.v23.css`、脚本递增为 `app.v28.js`（新增分享状态同步与分享/取消分享）；新增 `internal/app/community.go`、`internal/app/web/community.html`、`internal/app/web/community.v1.js`、`internal/app/web/style.v24.css`。新增 Go 测试：分享与社区可见、重复分享去重只刷新时间、取消分享与再分享、仅分享人可取消、上传 GIF 的校验（拒静态图/坏数据/非法编号）、键集分页与非法游标、未登录拒绝、账号合并后分享跟随并可取消、作品集到期清理不影响社区池、社区页渲染与 noindex。

## 2026-09-13 · 0.1.40

- 首页新增「示例作品」区（创作区之前、提示条之后）：展示两张真实生成的动图「给你比心」与「耶！成功啦」，说明一张自拍可生成的动图样式，并带「我也要做一支」入口直达创作区。
- 示例动图由原始 GIF（256×256、16 帧、约 1.4 秒循环，合计 605 KB）转为动画 WebP：`example-heart.webp` 73 KB、`example-success.webp` 106 KB，另各配一张无损静态首帧 `example-*-poster.webp`（12 KB / 17 KB）。
- 移动端优先的加载与展示：卡片先显示静态首帧，进入视口（提前 200px）后才挂上动画，离开视口即卸载停止解码，手机上不同时循环两张动图；≤800px 为横向滑动吸附卡片（第二张露出边缘提示可滑动、一屏约一张半），≥801px 才并排两列并锁 348px 卡片宽度（跟随 256×256 原始画幅，不拉伸变糊）；`prefers-reduced-motion: reduce` 时只保留静态首帧，不下载动画。
- 首页样式递增为 `style.v22.css`（基于主线现用的 `style.v20.css` 追加示例区规则，v21 属后台专用样式线，不复用），新增加载脚本 `showcase.v1.js`，index.html 引用同步更新；新增 Go 测试：页面引用与资源齐备、示例确为 16 帧动画 WebP 且首帧为静态图、动画体积上限、`/assets/` 正常提供并带 `image/webp` 类型。

## 2026-09-13 · 0.1.39

- 新增每日统计邮件：北京时间（Asia/Shanghai，UTC+8）每天凌晨 00:23 起汇总前一天的数据并发送，主题为「拾光 GIF 每日统计 YYYY-MM-DD」，内容包括新增用户数、消耗创作次数（先按定稿图/GIF 动图拆分，再按男生/女生/小朋友分类拆出「定稿图 x · GIF 动图 y」），以及次数来源（兑换码兑换：共 X 次（N 个兑换码）、注册赠送、后台增加）。
- 收件人与用户反馈通知一致：后台「反馈接收邮箱」，留空回落到发件邮箱；邮件服务未配置或发送失败不记标记，之后每分钟自动重试，服务在发送时刻后才启动的当天仍会补发，每个统计日最多成功发送一封（`settings.daily_stats_sent` 记录已发送的统计日）。
- 新增 `usage_stats` 表记录每次扣次消耗（任务类型、人物分类、动作与时间），与扣次同事务写入；失败退款（在线失败退款与服务重启中断退款）时同事务删除，统计口径为实际消耗。功能上线前的历史任务没有消耗记录，首个统计邮件的消耗数字只含上线后的部分。
- 新增 `internal/app/daily_stats.go`（时区与发送时刻判断、数据汇总、邮件正文生成、每分钟检查的定时器）；新增 Go 测试：发送时刻边界（00:22 不发、00:23 起统计前一天）、按窗口聚合各维度数据并发送到配置邮箱、同一统计日不重复发送、发送失败不记标记可重试、消耗随扣次写入且退款删除。

## 2026-09-13 · 0.1.38

- 管理后台新增“兑换记录”页签：集中展示账号获得次数的三类来源——注册赠送（创建账号时发放）、兑换码兑换（备注列为兑换码编号，可与“兑换码”页签对照）、后台手动增加，按时间倒序分页（每页100条），支持按账户名、邮箱或账号编号搜索，带“↻ 立即刷新”按钮。
- 新增 `credit_history` 表记录次数来源，三类写入与余额变动同事务：注册赠送在账号创建时写入（限流后零额度不产生记录）、兑换码在兑换成功时写入、后台增加在手动加次时写入（仅保存停用不产生记录）。
- 服务启动对旧库做一次性回溯（事务内完成并打标记，重复重启不会重复回溯）：兑换码兑换从 `codes` 精确还原、后台增加从审计记录（`user_update` 的 "账号 add=N disabled=t" 格式，无法解析或账号不存在则跳过）还原、注册赠送无法追溯原始值，按“剩余赠送 + 任务已消耗赠送次数”估算并在备注标注“历史估算”（合并过邮箱的账号可能略有偏差）。
- 新增接口 `GET /api/admin/credits`（管理员，支持 `q`/`page` 参数）；后台脚本递增为 admin.v22.js，admin.html 引用同步更新；样式不变（复用既有列表样式）。
- 新增 Go 测试：三类来源的实时记录、列表与搜索、零增加不记录；旧库回溯（含估算、跳过无效审计、重复重启不重复回溯）。

## 2026-09-13 · 0.1.37

- 管理后台移动端（≤600px）表格展示优化：单元格不再折行，超长内容（账号编号、失败原因、目标对象等）以省略号截断，表格容器保持横向滚动查看完整内容；每格宽度上限收紧为 160px。
- 操作列（账号“保存”、兑换码“复制/Mark”按钮）不截断不换行，保持完整可点：后台样式递增为 style.v21.css（基于后台现用的 style.v10.css 新增移动端 `.admin-table` 规则；v11–v20 为主线页面样式线，不含后台专用规则，不复用），后台脚本递增为 admin.v21.js（操作单元格加 `operation` 类）；admin.html 引用同步更新，桌面端展示不变。

## 2026-09-13 · 0.1.36

- 账号管理页签新增“↻ 立即刷新”按钮：与任务列表、任务历史一致，点击有缩放动画、刷新中禁用显示“↻ 刷新中…”、完成后短暂显示“✓ 已刷新 HH:MM:SS”，手动刷新保持当前搜索词与页码重新拉取账号列表。
- 后台脚本递增为 admin.v20.js（新增 `refreshUsers` 绑定，复用 `bindRefreshButton`）；admin.html 引用同步更新。

## 2026-09-13 · 0.1.35

- 管理后台各列表的创建时间固定按北京时间（Asia/Shanghai，UTC+8）展示，不再随管理员浏览器所在时区变化；覆盖任务列表、任务历史、兑换码、账号、操作记录五处列表。
- 后台脚本递增为 admin.v19.js（`timeText` 增加 `timeZone: "Asia/Shanghai"`）；admin.html 引用同步更新。

## 2026-09-13 · 0.1.34

- 任务总时长上限收紧为 20 分钟：从创建起（首轮提交等待 + 上游结果续查认领）超过 20 分钟即直接按失败收口（此前为单轮 8 分钟 + 续查 30 分钟合计 38 分钟）。新增常量 `jobTimeLimit = 20 分钟`，`upstreamRetention` 改为 `jobTimeLimit - generationTimeout`（首轮等待之外可续查 12 分钟），`New` 直接以 `jobTimeLimit` 设定 `waitBudget`；既有收口路径不变（续查轮次发现总时长耗尽、每分钟兜底清理超时的等待任务），失败原因仍为「等待上游结果超时，已按失败收口」并按退款配置退回次数。

## 2026-09-13 · 0.1.33

- 服务重启不再把在途任务一律置为「已中断」并退款，改为尽量恢复执行：已记录上游任务号的任务（上游提交已成功）转回“等待上游结果”，由调度器按原任务号继续认领，不重复提交、不产生第二次上游计费；其余任务只要输入文件仍在服务器，重新排队执行（仅出现在提交请求尚未被上游确认就中断的极小窗口，会重新提交一次上游请求，用户不会被扣第二次）；两者都不具备的（旧版本运行中任务的输入只在内存）才按中断收口并按退款配置退回次数。
- 任务输入（input.json）落盘时机调整：创建任务时无论直启还是排队统一先落盘（原先只有排队任务落盘），任务开始执行时不再删除，改为进入终态（成功/失败）后删除。由此同时修复一个既有缺陷：超时续查的任务此前在开跑时输入已被删除，续查成功后定稿凭证里的自拍哈希与造型为空，导致该定稿无法用于动作生成；现在续查成功可签发完整凭证。
- 恢复逻辑位于 `App.recoverJobs`（迁移之后执行），恢复任务的调度由既有 30 秒兜底通知触发，启动阶段不主动发起上游请求；等待上游结果的总认领时效照常对恢复任务生效，超时按失败收口退款。
- 测试更新：`TestQueuedJobInterruptedOnRestartRefunds` 改写为 `TestRestartRecoversJobsInsteadOfInterrupting`（输入缺失中断退款 + 输入仍在重新排队并执行成功）；新增 `TestRestartResumesUpstreamClaim`（重启后按原上游任务号续查、不重复提交、不重复扣次、凭证完整）。

## 2026-09-13 · 0.1.32

- 后台“立即刷新”（任务列表、任务历史）与“刷新记录”（操作记录）增加点击反馈：点击瞬间按钮有缩放动画并进入禁用态显示“刷新中…”，刷新完成后按钮短暂显示“✓ 已刷新 HH:MM:SS”（2秒后恢复），失败则恢复原样并提示错误，手动刷新是否生效一目了然。
- 后台脚本递增为 admin.v18.js；admin.html 引用同步更新；样式不变（复用既有 `button:disabled` 半透明样式与 Web Animations 实现按下动画）。

## 2026-09-13 · 0.1.31

- 触达单账号并发任务上限时的提示统一改为「已达任务上限X，请等待部分任务完成后再提交」（X 为后台配置的上限值）：服务端拒绝新任务的 409 文案与前台预检 toast 保持一致；409 属 user-facing 状态，前台直接展示该文案，不会落入「网络异常，请稍后重试~」通用提示。
- 首页脚本递增为 app.v24.js（新增 `concurrencyLimit()` 复用后台下发的上限值，`taskLimitReached`/`freeMotionSlots` 与两处预检提示改用它；预检提示从“当前已有 N 个任务”改为直接给出上限值）；后台与样式不变。
- `TestParallelJobsUpToUserConcurrency` 补充断言：并发 409 文案包含「已达任务上限2」。

## 2026-09-13 · 0.1.30

- 后台新增“动作序列图规格”配置：在原 4×4 共16格、每帧256×256 之外，可选 5×5 共25格、每帧128×128。配置存于 `settings.motionGrid`（`4x4`/`5x5`，旧配置空值回落 4×4），经 `/api/catalog` 下发 `motionGrid` 供前台使用；保存时校验非法取值，旧后台页面不带该字段时补默认 4×4。
- 生成与合成两侧统一按规格工作：动作提示词末尾附加“以此为准”的规格说明（覆盖模板里写死的格数与阶段划分，5×5 为 1—6准备、7—12展开、13—18重点、19—25收势）；服务器 `synthesizeGIF` 按规格切格合成 16/25 帧，展开阶段提速区间随帧数切换（第5—8帧 / 第7—12帧）。上游仍固定输出 1024×1024 方形序列图，5×5 无法整除时按比例取整划分格边界，每格相差不超过1像素。
- 首页脚本递增为 app.v23.js（本机合成按 `motionGrid` 切格、改用新增的 `gif-worker.v3.js`，合成提示按实际帧数显示）；`gif-worker.v1.js` 保持 16 帧原行为不变，供已分发的历史页面继续使用。后台脚本递增为 admin.v17.js（新增规格下拉框），admin.html 说明同步更新；样式不变。
- 新增 Go 测试：5×5 服务器切格合成（25帧、128×128、第7—12帧提速、1024 不可整除取整切分）、后台切换 5×5 后动作提示词附加以此为准的规格说明且产出 25 帧服务端 GIF、动作序列图规格非法取值被拒。

## 2026-09-13 · 0.1.29

- “最近的调用记录”失败条目新增失败原因展示：`/api/calls` 返回 `jobs.error_message`（空值补空串），前端在记录标题下方以小字红字显示“失败原因：…”。
- 上游报错 `Invalid image file or mode for image N, please check your image file.`（按前缀匹配，兼容 image 1/2/… 序号）时改写为“自拍照数据异常，请重新上传后尝试”，其余失败原因原样展示。
- 首页脚本递增为 app.v22.js、样式递增为 style.v19.css（新增失败原因样式）；后台、共享与分享脚本不变。
- “我的账号”与“最近的调用记录”弹窗支持点击遮罩区域关闭（等同关闭按钮）：按点击坐标是否落在弹窗边界外判断，避免误触弹窗内边距。
- “我的定稿”支持同一账号跨设备同步：此前定稿（含历史候选）只存在各设备本机 IndexedDB，服务器结果文件最多保留 3 天且无按账号的定稿列表，换设备后看不到其他设备的定稿。现在新增 `drafts` 表（定稿凭证为主键，含造型与图片，每账号保留最新 30 张），定稿生成成功时由服务端写入；新增 `GET /api/drafts`（列表）与 `GET /api/drafts/{receipt}/image`（图片，仅本人可读）；前端登录与关联邮箱后拉取云端列表，按定稿凭证合并进本机候选，本机无定稿时（如新设备）直接展示云端最新定稿；关联邮箱合并账号时云端定稿一并迁移。历史定稿（建表前生成）仍只保留在各设备本机。

## 2026-09-12 · 0.1.28

- 修复“有动作在制作时不能继续提交”的断点：动作批次不再互斥。原先前一批次（哪怕只提交了一个动作）结束前“让我的角色动起来”一直禁用，只能先点“停止提交新动作”才能再提交；现在有动作在制作时，勾选新的动作即可继续提交新批次，只受后台“单用户并发任务数”（默认5）与剩余次数限制，达到并发上限才禁用。
- 批次状态由单个 `motionBatch` 改为集合 `motionBatches`：每批只负责点击时选中的动作，各自按空闲并发额度提交、完成一个补一个；“停止提交新动作”一次性暂停所有批次里还没发出的动作，已经提交的继续查询到完成。
- 点击即把这一批动作从“已选”取下（连点两次不会重复提交同一批），对应动作卡片显示“制作中”；制作中的动作不计入“已选 N 个动作”，完成后恢复可选，想做第二次重新勾选即可。暂停、额度不足或失败的动作仍会放回选择里，方便稍后再发起或直接重试。
- 刷新恢复的任务同样把动作标记为“制作中”，避免恢复后被再次勾选提交造成重复创建；没有已选动作时提示“已有 N 个动作在制作中，可以继续勾选新的动作提交”，不再让人误以为被锁住。
- 首页脚本递增为 app.v21.js、样式递增为 style.v18.css（新增动作卡片“制作中”样式），首页“可以多选”说明与计费提示同步更新；后台、共享与分享脚本不变。
- `scripts/test-motion-parallel.mjs` 换成带最小DOM与接口替身的测试：保留原有的按额度提交、完成即补位、暂停保留未提交动作、并行宽度与整批预估断言，新增“有动作在制作/批次提交中不锁生成按钮”与“制作中动作有标记且不计入已选”，并端到端驱动两次 `makeMotions()`，验证动作制作中再挑一个动作能同时提交两个并行任务。

## 2026-09-12 · 0.1.27

- 修复两个一直失败的上游超时测试（`TestUpstreamResultClaimedAfterTimeout`、`TestUpstreamWaitWindowCollapsesToFailure`）：原先把认领窗口压到 2 秒甚至 400 毫秒，而轮询间隔固定 3 秒，窗口在第一次轮询前就耗尽，任务只能一路失败，测试必然不通过。
- 新增 `App.pollInterval`（由 `New` 设为新增的 `providerPollInterval`，默认仍是 3 秒）：测试可缩短轮询间隔，让“提交后多次轮询才拿到结果”在秒级内确定地走完；线上轮询节奏不变。
- 测试改为按持久信号判定：`pending_upstream` 是瞬时状态（调度器会立刻用同一上游任务号续查并改回 running），改用累计等待时长 `upstream_wait_ms` 确认任务确实进入过等待；续查成功与窗口耗尽收口都改为终态断言，保留“不重复提交、不重复计次、失败按配置退款”的校验。
- 顺带确定性覆盖：内存缓存失效（服务重启）后查询接口按数据库状态继续汇报等待，并带上 `upstream` 标记。

## 2026-09-12 · 0.1.26

- 回滚动图的补插、抖动与提速：合成 32 帧后动作显得过多、画面杂乱，恢复为 16 格源帧直接合成、共享自适应调色板、每帧 100ms（第5—8帧 60ms）的原有时序。
- 服务器合成 `synthesizeGIF` 与浏览器端编码算法同步回到该版本：移除帧间插值（预乘 alpha 混合与轮廓并集）、Floyd–Steinberg 误差扩散抖动与 20fps 帧率，恢复逐格取色与原有逐帧延迟。
- `gif-worker.v2.js` 改为转发 `gif-worker.v1.js` 的兼容壳：已分发的历史页面（app.v11–v19）仍引用该文件名，行为与 v1 一致，不会出现资源缺失。
- 首页脚本递增为 app.v20.js（本机合成提示文案恢复为“16 帧连续动作正在拼接”，并改为引用 `gif-worker.v1.js`）；后台与样式不变。
- 回滚 Go 测试中的帧数与时序断言（16 帧、100ms/60ms）；`scripts/test-gif.mjs` 改回 v1 编码器。

## 2026-09-12 · 0.1.25

- “让我的角色动起来”开放并发：多选动作不再一个一个串行等待，按账号并发额度（后台“单用户并发任务数”）一次性并行提交，额度用满后任意一个动作完成就立刻补提交下一个；超出服务器生成槽位的任务照常排队，页面可关闭稍后回来查看。
- 停止按钮语义改为“停止提交新动作”：只停止提交还没发出的动作，已经提交的动作继续查询到完成，未提交的动作保留在选择里，可稍后再发起。
- `/api/catalog` 新增 `generationSlots`（服务器同时执行的生成任务数上限，即 `cap(a.slots)`，默认2）；前台据此显示“N个动作并行生成（最多同时W个）”，整批用时按 `ceil(动作数 / 并行宽度)` 轮估算，不再把整批按串行累加。
- 并行提交的多个动作共用同一张自拍与定稿，图片数据整批只转换一次；任务写回 IndexedDB 的待恢复列表改为串行落盘，避免并行快照互相覆盖。
- 首页脚本递增为 app.v19.js（停止按钮文案与整批预估文案）；后台与样式不变。
- 新增 Go 测试锁定同一账号的两个动作可同时排队/生成并各自返回 GIF；新增 Node 脚本 `scripts/test-motion-parallel.mjs` 覆盖并行调度（按额度提交、完成即补位、暂停保留未提交动作、并行宽度与整批预估）。

## 2026-09-12 · 0.1.24

- 预估时间改为只保留聚合统计：新增 `generation_timing_stats`（按类型、模型、画质分组，保存成功样本数、实测最短/最长耗时与移动平均），不再保留每次请求的耗时；启动迁移会把旧 `generation_timings` 的逐次记录汇总成聚合值后删除该表。
- 预估算法调整：估计值取移动平均（`新平均 = 0.7 × 原平均 + 0.3 × 本次耗时`，最近一次成功调用占30%权重），参考区间在样本达到5次后直接取实测最短/最长，样本不足时按估计值上下30%（至少上下15秒）补足宽度；样本数不再限制为最近20次，失败调用不写入耗时样本。
- 去重改为 `jobs.timing_recorded` 标记（自动迁移），同一任务只计入一次，替代原来按任务编号去重的逐次记录表；`TimeEstimate` 新增 `minSeconds`/`maxSeconds`，调试日志 `timing_recorded` 增加最短/最长秒数。
- 首页脚本递增为 app.v18.js（预估文案改为“基于N次成功调用/历史参考”，样本数不再是近期窗口）；后台脚本递增为 admin.v15.js（任务列表展示成功样本数与实测最短/最长）；README 更新“动态预计时间”。
- 新增 Go 测试：聚合与分组隔离、实测最短/最长区间、聚合值持久化、旧表迁移汇总后删除且可重复执行。

## 2026-09-12 · 0.1.23

- 修复“上游超过8分钟仍没返回就一律失败”：提交成功时把上游任务号写入 `jobs.upstream_task_id`，任务转为新的 `pending_upstream` 状态，由调度器优先按**同一个上游任务号**继续认领结果（不重发生成请求、不重复计次），拿到图片后照常合成GIF/签发定稿凭证。
- 新增认领窗口：从任务创建起共38分钟（8分钟单次等待 + 30分钟续查），到期按失败收口并按后台配置决定是否退次数；等待中的任务仍计入单账号并发与全局2个生成槽位，服务器重启按既有规则标记 `interrupted` 并退回次数。新增周期30秒的调度兜底触发。
- `jobs` 新增 `upstream_task_id`、`upstream_wait_ms` 列（自动迁移）；`/api/jobs/{id}` 新增 `upstream` 字段；`provider.go` 拆出 `pollProvider` 并新增 `continueProvider`，续查不经过提交阶段。
- 调试日志新增 `job_waiting_upstream` 与 `provider_resume`，便于区分“上游还在生成”和“上游失败”。
- 修复续查任务在服务器重启后缺失 `input.json` 时 `runJob` 的空指针崩溃。
- 新增 Go 测试覆盖超时续查（同一上游任务号、只提交一次、不重复计次）与认领窗口收口退款。
- 首页脚本递增为 app.v17.js、样式递增为 style.v17.css（等待上游结果的状态文案），共享脚本递增为 common.v4.js；README补充“等待上游结果与超时续查”。

## 2026-09-12 · 0.1.22

- 生成前检查同账号是否已有配置完全相同的任务在排队或执行：命中时服务端返回409且不创建、不扣次，页面弹出二次确认，确认后才创建第二个任务并照常计次，取消则原任务继续执行。
- 新增 `jobs.dup_digest`（排除请求编号的配置摘要，覆盖自拍/定稿、画风、分类、服装、配色、武器与动作），启动迁移为旧的排队任务补写；空值时用原 digest 兜底比对。同一请求编号的重传仍走幂等分支直接返回原任务，不同账号互不拦截，也不对“刚创建”的任务放宽，连点两次同样会先提醒。
- 检测置于每小时生成限流之前，被拦下的重复请求不占用限流额度；重复创建仍受单账号并发上限与剩余次数校验约束。
- `common.v3.js` 保留错误响应中的附加字段（`error.api`），供页面按业务分支处理；首页脚本递增为app.v16.js、样式递增为style.v16.css（新增二次确认弹窗样式），后台与分享脚本改用common.v3.js。
- 新增 Go 测试锁定重复检测：同款配置换请求编号返回409、同请求编号重传复用原任务、`allowDuplicate` 确认后创建并计次、结束后不再拦截，以及账号与配置维度的边界。
- README补充重复提交的二次确认规则。

## 2026-09-12 · 0.1.21

- 放开“生成中不能再提交”的限制：同账号可同时执行多个任务（上限取后台“单用户并发任务数”，默认5），等待期间可以继续修改服装、配色或画风并提交新的定稿，不必等前一个任务结束。
- 用任务注册表取代单个 running 标记：每个任务独立提交、独立轮询、独立显示进度；进度区改为任务列表，每行显示自己的标题、说明与等待时间/参考剩余，多个任务不再互相覆盖。
- 并行完成的定稿只加入“我的定稿”列表，不打断当前操作；只提交一个定稿时仍自动展示结果。最后一个定稿完成后自动展示，与原有体验一致。
- 动作批次改为按批次串行且不再阻止并行定稿；批次启动时固定所用定稿与凭证，避免并行任务完成时切换定稿影响后续动作。停止按钮改为暂停当前批次。
- 前端并发与次数双重校验：已提交未完成的任务同样计入次数占用，达到并发上限时禁用生成按钮并提示，服务端仍会再次校验并返回409。
- IndexedDB 的 pending 记录改为任务数组，刷新或重开页面后逐个恢复所有未完成任务；兼容旧版本存放的单个任务记录，恢复时不会重复创建或重复扣次。
- 新增 Go 测试锁定服务端行为：同账号可并行提交到并发上限，超出返回409。
- 首页资源递增为app.v15.js / style.v15.css；README补充并发说明。

## 2026-09-12 · 0.1.20

- 已生成的角色定稿从第2步“确认专属角色”内提取出来，改为“开始你的小小创作”大框架内、步骤标签下方的“我的定稿”列表，第1、2、3步都可见，不再受当前步骤限制。
- 只要生成过定稿就显示列表（含仅一张时），标题旁标注“已生成 N 张 · 点击切换到对应定稿”，点击缩略图切回对应定稿并进入第2步；修复已确认定稿后重进页面直接进入第3步时看不到历史定稿的问题。
- 定稿列表独立渲染（恢复造型、生成新定稿、切换定稿时同步），不再依赖定稿详情视图的渲染时机；README补充说明。
- 首页资源递增为app.v14.js / style.v14.css。

## 2026-09-12 · 0.1.19

- 首页页头“分享”右侧新增“反馈”入口（窄屏收成图标按钮），弹窗内最多填写200字并实时显示字数，空内容不可提交。
- 新增 `POST /api/feedback`：内容去除首尾空白后校验1—200字，单账号每小时3条、单IP每小时10条；未配置SMTP返回503，发送失败返回502，`/api/catalog` 通过 `feedbackConfigured` 告知前台入口是否可用。
- 反馈按后台“邮箱发送配置”的SMTP账号发送通知邮件，收件人取新增的“反馈接收邮箱”（留空发送到发件邮箱）；主题为`拾光 GIF 用户反馈`，正文含反馈原文、时间、账号名、账号编号与绑定邮箱。
- 邮件组装抽出 `mailMessage`：邮件头强制单行、非ASCII主题按RFC 2047编码、正文换行统一为CRLF并声明8bit编码，验证码邮件沿用同一通道且内容不变。
- 反馈原文同时写入 `feedback_sent` 审计记录，后台“审计记录”可直接复查；README补充反馈入口说明与数据边界。
- 首页资源递增为app.v13.js / style.v13.css，后台递增为admin.v14.js。

## 2026-09-12 · 0.1.18

- 上传自拍后新增画风选项：默认、Q版、水墨风格三项，默认画风保持原有轻度Q版效果。画风同时作用于角色定稿与动作图，并写入定稿凭证；动作阶段必须与定稿一致，改画风须重新生成定稿。
- 画风提示词紧跟“照片处理 / 人物特征保留”提示词之后加入定稿与动作两阶段提示词，并声明与前文冲突时以画风为准。请求未带画风时按默认画风处理（兼容旧页面与旧记录），未知编号返回400；`/api/catalog` 只下发编号、名称、说明与图标，不返回提示词内容。
- 后台“角色模板与各步骤提示词”新增三种画风提示词编辑，不可增删；兼容旧数据：缺少画风字段的配置读取后自动补入默认值，旧后台页面提交不含画风的配置也不会清空画风。
- 首页脚本递增为app.v12.js，样式递增为style.v12.css（新增画风卡片样式，移动端同步适配），后台脚本递增为admin.v13.js；首页创作说明、FAQ与SEO描述同步补充三种画风。

## 2026-09-12 · 0.1.17

- 新增 `scripts/clean_disk.sh`：磁盘不足或部署卡死时释放空间。默认清理Go构建缓存、构建中断残留的`gif-server.next`与本机`*.exe`、`logs/`下超过2天的`auto_deploy-*.log`，以及npm下载缓存、apt缓存和systemd journal；`--aggressive`追加Go模块缓存、snap缓存、轮转系统日志与超量自动备份；`--dry-run`只统计不删除，`--no-system`只动应用目录。
- 新增 `scripts/check_deploy_version.sh`：用`.last_deployed_commit`状态文件与`origin/<branch>`比较，输出up-to-date(0)、newer-commit-available(3)、stuck(4)、unknown(1)与用法错误(64)，非零状态附中文处理提示；`--quiet`只输出状态关键字。二进制不支持`version`子命令，已部署提交只能来自状态文件。
- 两个脚本都要求应用目录存在`go.mod`或`.git`，且永不删除`gif.db`（含`-wal`/`-shm`）、`gif-server`、`gif-server.previous`、`.env`、`node_modules/`、`work/`与源码；系统清理在无root且无免密sudo时跳过并warning。
- 新增自包含、无网络、打桩系统命令的`scripts/tests/clean_disk_test.sh`与`scripts/tests/check_deploy_version_test.sh`；README补充“磁盘清理与部署版本检查”小节及`/var/backups/gif`保留策略现状（仍由`scripts/backup_database.py`只保留最近2份自动快照，本脚本不改变该策略）。

## 2026-09-12 · 0.1.16

- 上传自拍照成功后立即存入本机IndexedDB（沿用profiles记录），关闭网页后重新进入会自动展示上次上传的照片，可直接使用或换一张；本地保存失败仅提示，不影响本次上传。
- 恢复逻辑兼容仅存照片、尚未生成定稿的记录，也覆盖生成任务进行中重进页面的场景（从pending任务恢复照片预览）。
- 首页脚本递增为app.v9.js，前端时长预估冒烟脚本默认指向app.v9.js。

## 2026-09-12 · 0.1.15

- 补齐全功能测试套件：结果文件3天过期清理与过期任务汇报、任务越权与中断状态、排队任务重启退回、排队输入丢失按失败退回、限流窗口重置、请求解码校验、全量配置校验分支、管理后台用户搜索/加次/停用（含会话清除）、邮箱配置校验、旧任务表索引升级迁移。
- 前端时长预估冒烟脚本默认指向 app.v8.js，与服务器后台任务流程保持同步。

## 2026-09-12 · 0.1.14

- 生成任务改为服务器后台执行并支持排队：槽位占满时任务入队等待（不再返回繁忙），由调度器按创建顺序启动；创建后可关闭网页，重新进入页面沿用原任务的倒计时与等待时间。
- 定稿与动作GIF改由服务器合成（算法与浏览器端一致：共享自适应调色板、透明背景、逐帧时序），结果文件保存在数据库同目录files/下，最多保留3天，过期自动清理；getJob在内存缓存失效后仍可从文件恢复结果与定稿凭证。
- 管理后台新增“任务列表”：展示排队中与进行中的任务（类型、动作、账号、创建时间、已等待、预计耗时），8秒自动刷新，已完成任务不再显示。
- 首页脚本递增为app.v8.js，后台脚本递增为admin.v8.js；更新首页/FAQ中关于图片保存与合成方式的说明。

## 2026-09-12 · 0.1.13

- 自动部署日志改为按天保存：auto_deploy.sh自行写入logs/auto_deploy-日期.log（Asia/Shanghai），并删除2天前的日志，只保留今天与昨天。
- cron条目不再由shell重定向到单一auto_deploy.log，需重跑install_cron.sh更新既有crontab；旧auto_deploy.log不再追加，可手动清理。

## 2026-09-12 · 0.1.12

- 兑换弹窗中“获取兑换码”区域支持点击整块直接复制内容，优先使用剪贴板API，受限环境回退execCommand，并给出复制成功/失败提示。
- 区域增加指针光标、悬停高亮和“点击此区域即可复制”提示；内容为空时仍隐藏、点击无动作。首页脚本递增为app.v7.js，样式递增为style.v8.css。

## 2026-09-11 · 0.1.11

- 后台增加“获取兑换码内容”配置，支持多行文字与emoji，保存后同步展示在前台兑换弹窗；留空隐藏，最多2000字。
- 内容以纯文本安全展示，避免执行HTML；沿用后台权限、CSRF和配置同步机制。
- 首页/后台脚本分别递增为app.v6.js和admin.v7.js，首页样式递增为style.v7.css。

## 2026-09-11 · 0.1.10

- 自动部署在替换应用二进制和重启前创建SQLite一致性备份，包含WAL中已提交的数据，并校验备份完整性。
- 默认保存到/var/backups/gif，只保留最近2份gif-auto自动快照；备份或清理失败中止部署，备份成功后才清理旧快照，不删除手动或其他项目备份。
- 首次尚无数据库时记录跳过；新增Python 3 SQLite备份能力预检及备份/保留策略测试。

## 2026-09-11 · 0.1.9

- 页脚新增“联系我”，点击弹窗展示用户提供的微信二维码，支持保存原图，点击时才加载图片。
- 二维码原文件不压缩、不重新生成，加载异常沿用统一提示；样式版本递增为style.v6.css。

## 2026-09-11 · 0.1.8

- 接口、网络、生图、GIF合成、本地存储和分享资源加载等异常统一提示“网络异常，请稍后重试~”，不再向用户拼接内部原因、排查编号或扣次后缀。
- 保留必填项、文件格式、额度和兑换码等正常业务校验提示，服务端详细错误仍写入脱敏debug日志。
- 更新公共错误处理模块及首页、后台、分享脚本版本。

## 2026-09-11 · 0.1.7

- 为定稿和动作生成增加预计用时、参考范围、已等待时间及多动作队列用时展示，超出估计时提示仍在处理，不显示虚假完成倒计时。
- 按生成类型、模型、画质记录每次完成调用的实际耗时。使用最近20次成功调用的指数移动平均动态更新预估，失败单独记录，不拉低成功时间预估；统计持久化且按任务去重。
- 页面恢复任务时使用服务端已耗时校正计时；暂无历史时标注初始估计。首页脚本递增为app.v4.js。

## 2026-09-11 · 0.1.6

- 新增GIF_DEBUG开关，按任务编号串联图像请求摘要、提交/轮询HTTP状态、DNS/TCP/TLS、上游任务及request-id、图片下载和扣次结果。
- 解析并保留上游HTTP错误、异步任务失败错误码/消息以及原始网络失败原因；日志过滤密钥、提示词、图片Base64和签名URL，不记录完整请求/响应正文。
- 失败页面显示排查编号；补充错误响应、网络错误、日志开关和敏感内容遮蔽测试，不增加真实生图请求或自动重试。

## 2026-09-11 · 0.1.5

- 照片不超过5MiB时原样上传，保留原始文件、尺寸、格式和元数据；仅超过5MiB才进行浏览器压缩处理。
- 服务端补齐原始WebP校验与常见高分辨率照片支持，保留文件大小和像素安全上限；同步首页提示、FAQ与文档。首页脚本递增为app.v3.js。

## 2026-09-11 · 0.1.4

- 修复后台配置图像服务后首页仍缓存“未配置”、导致生成角色按钮持续禁用的问题。保存配置后跨页面通知，并在页面恢复焦点时同步服务状态，保留已选照片和造型。
- 增加按钮旁的服务状态说明和手动重新检查入口；生成前刷新配置、会话CSRF与次数，未就绪时不上传图片、不扣次数。
- 首页脚本递增为app.v2.js，后台脚本递增为admin.v5.js。

## 2026-09-11 · 0.1.3

- 首页新增分享入口，提供包含emoji和正式网站链接的推广文案，支持复制及剪贴板受限时手动复制。
- 新增1080×1440 PNG推广海报，内含直达https://gif.jcc666.top/的二维码，支持预览和下载；生成脚本使用独立解码器核验二维码。
- 分享不依赖登录或图片接口，不消耗创作次数；海报按需加载。样式版本递增为style.v5.css。

## 2026-09-11 · 0.1.2

- 修复内置浏览器不支持prompt()导致旧码复制报错的问题：改为页面内补录弹窗；剪贴板受限时提供可手动复制的弹窗，取消或失败不改变Mark状态。按钮文案统一为“Mark”，后台脚本递增为admin.v4.js。
- 角色动作改为名称与过程成对的编辑卡片，桌面每行3张，支持新增/删除并保留稳定动作编号；每类1—12个动作。
- 兑换码新增复制操作，Mark时先复制再更新状态，已Mark行与按钮变色。新码加密保存，旧码支持校验后补录；复制接口沿用管理员权限与CSRF校验。
- 首页新增可见GIF制作说明与FAQ，补齐canonical、Open Graph、JSON-LD结构化数据、robots.txt、sitemap.xml、llms.txt；后台/API禁止索引。
- SEO内容与可见页面一致，不加入隐藏指令提权文本；样式递增为style.v4.css，后台脚本为admin.v3.js。

## 2026-09-11 · 0.1.1

- 移除首页管理后台链接，管理登录页面改用 `/who`；原 `/admin` 和静态HTML旁路不再提供管理页面。
- 兑换码支持未兑换、已Mark、已兑换三种状态，管理员可Mark或取消Mark，已兑换状态优先且禁止再次标记。
- Mark不影响正常兑换、有效期或次数；标记接口沿用管理员权限、CSRF校验与审计记录。
- 自动迁移已有兑换码数据，新增编号便于对应导出清单；后台脚本递增为admin.v2.js。

## 2026-09-11 · 0.1.0

- 新增拾光GIF Web项目，提供暖色响应式创作页面与管理后台；当前样式版本v3。
- 实现设备账号、默认5次额度、邮箱验证与跨设备账号关联、永久单次兑换码、管理员额度管理。
- 内置男生古代铠甲、女生可爱日常、小朋友童趣三套角色模板，各6种动作，可管理服装/配色/武器和各阶段提示词。
- 接入GeekAI GPT Image 2.5异步图片接口，提供定稿确认、逐个动作生成、幂等扣次、全局/账号并发限制。
- 浏览器压缩上传、IndexedDB本机作品库、Web Worker透明GIF编码、刷新恢复和下载。
- 加入CSRF、参数化SQL、限流、敏感配置加密、图片与URL校验、SSRF防护、CSP以及管理审计。
- 沿用card的systemd/Nginx/Certbot/五分钟main自动部署方式，新增部署锁、健康检查及二进制回滚。
- 增加额度/并发/幂等/兑换/邮箱合并/权限/图片校验测试，以及GIF编码与标准解码器交叉验证。
- 验证边界：本地浏览器使用合成图片测试；真实GeekAI、SMTP投递及公网服务器上线须在配置对应服务后验证。
